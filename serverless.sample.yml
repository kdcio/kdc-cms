service: ${self:custom.name}

provider:
  name: aws
  runtime: nodejs10.x
  stage: ${opt:stage, 'dev'}
  region: ap-southeast-1
plugins:
  - serverless-nextjs-plugin
custom:
  name: kdc-cms
  serverless-nextjs:
    assetsBucketName: "kdc-cms-${self:provider.stage}"
package:
  exclude:
    - ./**
